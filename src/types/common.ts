export interface MockType {
  type: string;
}

export interface TodoContent {
  id: string;
  title: string;
  description: string;
}

export interface AlbumImage {
  width: number;
  height: number;
  url: string;
}
